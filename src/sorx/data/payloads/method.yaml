# Since the method is hardcoded in cors.py, idk wtf I'm supposed to do with this, but I'll just keep it here.
method:
  high_priority:
    - "GET"
    - "POST"
    - "PUT"
    - "DELETE"
    - "PATCH"

  medium_priority:
    - "HEAD"
    - "OPTIONS"

  low_priority:
    - "TRACE"
    - "CONNECT"
    - "ACL"
    - "PROPFIND"